# Appeal Engine — product demo

Static, clickable demo of the Appeal Engine product screens (Medicare Advantage
concurrent-review support for skilled nursing facilities). All data is fictional;
nothing is sent anywhere. State is saved locally in your browser — use "Reset demo"
in the top bar to start over.

Built from the design-canvas screen modules; no backend.
